export const sitekey = process.env.SITEKEY;
export const authors_path = (letter) => `/authors${ letter ? `?letter=${letter}` :""}`;
//export const authors_path = (letter) => `/${sitekey}/authors${ letter ? `?letter=${letter}` :""}`;